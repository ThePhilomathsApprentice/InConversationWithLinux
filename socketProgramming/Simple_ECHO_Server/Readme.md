Echo server
-Only Echo the client messages.
-Should use Multiple threads.
-Should handle kill Signals and destroy all the server sockets and threads as needed.
-Should be able to handle many connections at once.
-Should have the option to configure port number using the command line option ( -p ).